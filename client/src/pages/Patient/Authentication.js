import React from "react";
import AuthenticationForm from "../../components/Patient/Authentication";

const Authentication = () => {
  return <AuthenticationForm />;
};

export default Authentication;
